{
  "name": "lanaclara-mobile",
  "version": "0.1.0",
  "private": true,
  "description": "App móvil (Capacitor) que envuelve LanaClara — Android ahora, iOS después",
  "scripts": {
    "sync": "cap sync",
    "open:android": "cap open android",
    "apk:debug": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.1",
    "@capacitor/cli": "^8.4.1",
    "@capacitor/core": "^8.4.1"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
